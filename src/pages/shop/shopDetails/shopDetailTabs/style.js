import { makeStyles } from '@mui/styles'

const useStyles = makeStyles((theme) => ({
  root: {
    // paddingBlock: 20,
    paddingInline: 20,
    marginBlockStart: 20,
    background: '#fff',
    borderRadius: 5,
  },
}))

export default useStyles
